{
  "name": "top-down-room-game",
  "version": "1.0.0",
  "description": "A small extensible top-down browser game with loot and equipment.",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "start:tauri-dev": "npx serve . -l 4173",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "test": "node --test tests/*.test.js",
    "test:soul-siphon": "node --test tests/soul-siphon.test.js",
    "test:progression": "node --test tests/progression-report.test.js",
    "report:progression": "node --test tests/progression-report.test.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "playwright": "^1.58.2"
  }
}
